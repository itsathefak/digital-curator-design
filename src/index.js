import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // Optional if you have any basic CSS
import App from "./App"; // Import your main App component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
